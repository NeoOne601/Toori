Shared design system, performance polish, viral moments
✅ Pass 1 — SmritiKit is in place under smriti-kit, with shared files at Package.swift, SmritiAPI.swift, SmritiModels.swift, SmritiEventStore.swift, SmritiDesign.swift, and SmritiImageCache.swift. I also fixed both Xcode projects to link the local package product in macOS project.pbxproj and iOS project.pbxproj.
✅ Pass 2 — performance work is in place. The iOS websocket lifecycle hardening lives in SmritiEventStore.swift: background/foreground pause-resume, clean close, heartbeat ping, and reconnect logic stay off the main actor. I also fixed two integration issues in PulseView.swift and RecallSheet.swift: the bloom now only fires when visible Pulse orbs actually match, and the hum reveal uses CGFloat state plus a fallback mic origin when layout hasn’t produced a button frame yet.
✅ Pass 3 — the viral moments are wired up. macOS “first surprise” is implemented in RecallView.swift: first-session surprise_score > 0.75 detection, banner slide-in/out, thumbnail/label copy, haptic, and best-effort menu bar icon pulse. iOS “memory bloom” is in PulseView.swift, and the “hum match” full-screen reveal is in RecallSheet.swift.
Verification: xcodebuild -project /Users/macuser/toori/mobile/macos/SmritiApp/SmritiApp.xcodeproj -scheme SmritiApp -destination 'platform=macOS' -derivedDataPath /Users/macuser/toori/.xcode-derived/smriti-macos-final build succeeded, and the resolved package graph included local SmritiKit. iOS package resolution also works, but this machine currently has no registered iOS Simulator platform or iPhone 16 Pro device: the escalated build failed at destination selection, and list_sims returned no enabled simulators. So the remaining blocker is environment-side simulator availability, not package resolution.
One important caveat: SmritiKit is now created and linked, but because the allowed file scope only let me update the project files and a small set of views/store files, the app-local SmritiAPI.swift / SmritiModels.swift copies still remain in the two app trees. The package is resolved locally and built, but the “single source of truth” extraction is only partial until those app sources are switched over in a follow-up pass.
