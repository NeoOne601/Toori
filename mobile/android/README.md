# Android Project Notes

This directory now contains a Gradle root project plus the `app` module.

Open `mobile/android` in Android Studio and let Studio:

1. install the configured Android Gradle Plugin if needed
2. generate or update the Gradle wrapper
3. download Android SDK components

The runtime URL in the current source defaults to `http://10.0.2.2:7777` for emulator use.
